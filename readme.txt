To run the app:

1. install python and Flask framework, http://flask.pocoo.org/docs/0.10/installation/ along with Mysql
2. use user.sql to create table in Mysql DB
3. navigate to Registration app folder
4. to run the server, run 'python app.py'
5. navigate to http://localhost:5000/ to see the registration page